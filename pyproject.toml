[project]
name = "startup"
version = "0.1.0"
description = "OmooLab startup files and keymap presets for Blender"
readme = "README.md"
requires-python = ">=3.11"
license = "GPL-3.0-or-later"
dependencies = []

[tool.uv]
package = false
